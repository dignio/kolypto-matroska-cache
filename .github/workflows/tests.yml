name: Test
on:
  # https://help.github.com/en/actions/reference/events-that-trigger-workflows
  pull_request:
    types: [ opened, edited, reopened ]
  release:
    types: [ prereleased, released ]
  push:
    branches: [ master, development ]
  # Trigger this workflow manually from the `actions` page
  workflow_dispatch:
    inputs:
      git-ref:
        description: Git Commit or Branch (Optional)
        required: false
jobs:
  tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    #container:

    strategy:
      matrix:
        python-version: ['3.12', '3.13', '3.14']

    name: ${{ github.ref }}, Python ${{ matrix.python-version }}
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - uses: actions/cache@v5
      with:
        path: ./.nox/  # what we cache: nox virtualenv (they're expensive)
        key: ${{ runner.os }}-nox-3-${{ matrix.python-version }}-${{ hashFiles('**/pyproject.toml', '**/noxfile.py') }}  # cache key
        restore-keys: |
          ${{ runner.os }}-nox-3-${{ matrix.python-version }}-
    - run: pip install nox
    - run: nox --python ${{ matrix.python-version }} --report nox-report.json
